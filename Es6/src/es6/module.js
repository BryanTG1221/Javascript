/* Modulos son logicas separadas del la aplicacion principal en la cual se puede llamar cuando se necesite*/

const hello = () => {
    return 'Hello'
}
 
/*Especificamos que exportamos la const "hello" para usarla en otro archivo */
module.exports = hello;



